---
名称: <页面名>
路由: /user/<页面>
模块: M2
优先级: P0
状态: 缺失
更新: <YYYY-MM-DD>
---

<!--
页面 README 的 v3 骨架。用法:复制本文到 M2-用户管理/<编号>-<页面名>/README.md,
把所有 <> 占位符换掉,删掉不适用的示例行。

规则的完整定义在 README.md,机械约束由 校验.py 强制。
本文只做骨架 —— 凡是校验器能测的,这里预置好位置;凡是要判断的,行内注释提示。
提交前跑一次: python3 00-公共约定/校验.py

需求编号必须是「文件夹编号-R<n>」。文件夹叫 2.4-封禁列表,编号就是 2.4-R1、2.4-R2。
-->

# <编号> · <页面名>

## 1. 背景与目标

<!-- 面向所有人,含老板。不得出现文件名、路径、接口名、表名等技术细节。
     写用户当前的痛,不写系统现状。 -->

**目标**:<一句话,可度量>

**谁在用**

| 角色 | 用来做什么 | 没有会怎样 |
|---|---|---|
| <角色> | <场景> | <代价> |

**业务价值**:<省了什么 / 防了什么 / 赚了什么>

**不做什么**

- <明确排除的范围,防蔓延>

**适用范围**

| 维度 | 取值 |
|---|---|
| 终端 | <桌面后台,最小宽度 1440> |
| 响应式 | <是否需要断点> |
| 界面语言 | <中文 / 葡语 / 双语> |
| 时区 | <GMT−3 · America/Sao_Paulo> |
| 货币 | <BRL,符号 R$> |

## 2. 入口

- 菜单:<一级> → <二级>
- 跳入:<从哪些页面能进来>

## 3. 术语

<!-- 只写本页特有词。通用词引模块 README 的术语表,资金类术语(余额、真金、彩金、
     打码任务、有效投注、闪兑)引 2.1 的 资金模型.md,一律不重复定义 -->

| 术语 | 定义 |
|---|---|
| <词> | <业务语言定义> |

## 4. 关键参数与假设

<!-- 所有阈值的唯一出处。其余段落和图一律写「见 A1」,不重复数值。
     规则数值本身放配置,这里只记:有哪些规则、语义、在哪配、谁能改。 -->

| 编号 | 参数 | 取值 | 依据 | 在哪配 |
|---|---|---|---|---|
| A1 | <参数名> | <值> | 已验证 / **未验证假设** | <配置位置 / 写死> |

## 5. 字段清单

**筛选字段**

| 字段 | 类型 | 可输入数据与校验 |
|---|---|---|
| <字段> | <text / select / daterange / number> | <格式、长度、上下限> |

**表格列**

| 列 | 说明 | 默认显示 |
|---|---|---|
| <列名> | <含义> | 是 / 否 |

## 6. 需求清单

<!-- 索引层:扫一眼知道有哪些需求、要什么权限。不写人日和现状。
     触发条件必填,写简写;完整版在第 7 段。编号稳定,不复用不重排。 -->

| ID | 需求 | 触发条件 | 验收标准 | 权限码 |
|---|---|---|---|---|
| <编号>-R1 | <需求名> | <按钮点击 / URL 直入 / 定时任务 / 外部回调> | <可测的结果> | `<模块:对象:动作>` |

## 7. 需求详述

<!-- 规格层。涉及资金、权限、外部系统,或处理逻辑有分支的需求必须展开;
     其余在第 6 段一行即可。展开的编号必须在第 6 段存在。 -->

### <编号>-R1

**触发条件**:<详列所有触发行为>

**可输入数据**:<类型、格式、长度、上下限>

**功能范围**

| 归属 | 做什么 |
|---|---|
| 界面 | <表单交互、即时提示> |
| 服务端 | <校验、权限、取数、脱敏> |

**处理逻辑**

1. <步骤>
2. <步骤>

**错误处理**

| 错误状况 | 提示 |
|---|---|
| <状况> | <提示文案> |

**测试案例**

- 正向:<输入> → <预期>
- 逆向:<异常输入或越权> → <预期拒绝方式>

## 8. 数据流向与系统串接

**数据流向**:<用户输入> → <服务端处理与写入点> → <界面显示>

**系统串接**:<涉及的第三方、对接人、正向与逆向流程、责任边界>
<!-- 无外部系统也要显式写「无外部系统」,不能省 -->

## 9. 异常与边界

<!-- 业务反流程,不是 UI 状态。越权、并发、超限、脏数据、幂等。
     每条应能对应一个测试用例。少于 3 条校验器报错。 -->

| # | 场景 | 触发条件 | 预期处理 |
|---|---|---|---|
| E1 | <越权> | | |
| E2 | <并发或重复提交> | | |
| E3 | <超限或脏数据> | | |

## 10. 状态清单

<!-- UI 呈现态,与第 9 段的业务反流程区分开。
     空/加载/错误/无权限四态是全局默认,不要抄一遍。
     只写两类:本页的表现差异、本页特有态。
     两类都没有就写「四态按全局默认」,删掉下面的表。 -->

| 状态 | 表现 |
|---|---|
| 错误 | <表现差异:卡片级还是整页,能否重试> |
| <特有态> | <数据未成熟 / 参数过大折叠 / 已下线仍显示历史> |

## 11. 涉及表与职责

**数据归属**

- 拥有(owner=M2):<本页写入的数据>
- 只读:<外部数据>(owner=<M2 / 本仓库范围外的哪个能力>)

**前后端职责**

| 事项 | 归属 |
|---|---|
| 筛选校验、结果正确性、超时保护 | **服务端** |
| 权限判定与可见范围过滤 | **服务端** |
| 敏感字段脱敏 | **服务端** |
| 表单交互、即时格式提示 | 界面 |

## 12. 关联页面

- 跳出:<本页能去哪>
- 跳入:<哪里能到本页>
