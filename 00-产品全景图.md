# Bingo 产品全景图

> 一页看懂整个产品。新人、CEO、外部合作方的第一入口。
> 结构基于**自身代码**(`bingo-backend/prisma/models`、resolvers)+ 竞品参考(`参考资料/竞品-aigobet`)。
> ⚠️ 思维导图表达**结构**;跨模块的**依赖关系**看下方第二张流程图——两张配套才完整。

---

## 一、功能全景(结构)

```mermaid
mindmap
  root((Bingo 平台))
    C端 玩家产品
      账户
        注册登录
        KYC 实名分级
        资金安全 密码
        身份标签
      钱包
        充值 法币与加密
        提现 与审核
        真金余额 balance
        彩金余额 bonusBalance
        冻结余额 待建
        账变流水
      游戏
        原创 8 款
          dice crash limbo plinko
          roulette mines baccarat blackjack
        第三方聚合 GamePlatform
        公平性 Fairness
        投注记录 BetRecords
      活动
        充值类 首充 累充 转盘
        留存类 签到 通行证 返现
        召回类 PWA
        打码类 返水 洗码
        裂变类 邀请 宝箱
      VIP
        充值VIP
        投注VIP
      推广裂变
        邀请关系
        代理分佣
      内容触达
        Banner 弹窗
        站内信 推送
    B端 运营后台
      数据中心
        大盘与渠道报表
        玩家分析 留存 流失 LTV
        游戏与输赢统计
      用户管理
        用户列表 详情
        投注明细 账变记录
        封禁 标签 点控
      玩法中心
        游戏大厅配置
        活动配置 引擎
        VIP 与代理配置
      财务管理
        充值订单 商品配置
        提现订单 审核规则
        通道 与 冻结解锁
      运营管理
        素材 消息 推送
        兑换码
      渠道管理
        渠道账号
        PWA 三方上报
      权限
        账号 角色
        操作日志
    平台横切能力
      多币种 多语言
      多渠道 白标
      风控
        反薅羊毛
        多账号 设备IP
        地理限制
      对账与审计
```

---

## 二、钱的一生(依赖关系)

这是全平台**最重要的一条链**,也是资损最集中的地方。思维导图画不出它,因为它横跨钱包/活动/游戏/代理/财务五个域。

```mermaid
flowchart TD
    A[用户充值] --> B[真金入账 balance]
    B --> C{触发活动?}
    C -->|是| D[发放彩金 bonusBalance]
    C -->|否| E[可直接投注]
    D --> F[挂打码要求<br/>⚠️ 字段缺失]
    F --> G[投注 BetRecords]
    E --> G
    G --> H[流水累计]
    H --> I{打码达标?}
    I -->|是| J[彩金解锁 → 转真金]
    I -->|否| F
    H --> K[VIP 进度]
    H --> L[返水 / 洗码]
    H --> M[代理佣金]
    J --> N[提现申请]
    B --> N
    N --> O{审核规则}
    O -->|自动过审| P[出金]
    O -->|转人工| Q[人工审核]
    Q -->|通过| P
    Q -->|拒绝| R[退回 / 冻结<br/>⚠️ 字段缺失]
    R --> S[解冻条件<br/>充值/登录/下级充值/后台]
    S --> B

    style F fill:#ffe6e6,stroke:#d33
    style R fill:#ffe6e6,stroke:#d33
```

### 图上两个红块 = 当前真实缺口
| 缺口 | 现状 | 后果 |
|---|---|---|
| **打码进度** | `Wallet` 有 `balance` / `bonusBalance`,但**无 wagering/turnover 字段** | 彩金无法挂流水要求 → 送出去的彩金可直接提走,薅羊毛入口 |
| **冻结余额** | 只有账号级 `fundLocked`,**无金额级冻结字段** | 提现拒绝后无法冻结部分金额;解冻的多条触发路径无处落地 |

> 参考:竞品 action_type 里 `97 提现审核拒绝并冻结` / `98 充值解冻` / `99 登录解冻` / `100 直属下级充值解冻` / `101 后台解冻`——五条解冻路径,说明这是成熟平台的标配。

---

## 三、模块依赖方向(架构约束)

```mermaid
flowchart LR
    user[user 用户] --> wallet[finance 资金]
    game[game 游戏] --> wallet
    activity[activity 活动] --> wallet
    activity --> user
    agent[agent 代理] --> wallet
    agent --> user
    cms[cms 内容] -.-> activity

    style wallet fill:#e6f0ff,stroke:#36c
```

**铁律:`finance` 不得反向依赖 `activity`。** 资金域只认"一笔账变",不认"这是哪个活动发的"——活动信息作为元数据传入,不是编译期依赖。违反这条,活动一改财务就得跟着改。

---

## 四、待拍板决策(挂在图上,别丢)
1. **自营 vs 白标哪条主线**——`bingo-backend`(NestJS)与 `bingo-saas`(Go 微服务)是替代还是并行?
2. **双 VIP 是否合并**——充值VIP / 投注VIP 两条独立线,用户会有两个 VIP 维度。
3. **身份体系 SSOT**——`UserKyc` / `UserTag` / `UserIdentity` 三套并存,谁是主?

> 这三条属于 `01-product-definition/`,是全图的前提。定不下来,下游一切返工。
