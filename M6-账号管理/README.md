# M6 · 账号管理

## 1. 定位与边界

**做什么**:后台账号、角色与权限的管理,以及全站操作日志。
**本模块是全平台权限模型的唯一定义方** —— 其余模块只声明自己有哪些权限节点,不重复描述模型。

**不做什么**
- 不做玩家账号(那是 M2 用户管理,后台账号与玩家是两回事)
- 不定义各业务模块具体有哪些节点(各模块在自己的 README 第 6 段声明,本模块只定模型与规则)
- 不做审批流 —— 开号、改角色即时生效,事后靠操作日志复核
- 不做登录认证流程本身(登录页、找回密码归玩家端与基础设施)

**铁律**
- **权限判定一律在服务端**。权限树同时被用来渲染菜单与按钮,
  但界面隐藏只是体验,不构成防护 —— 每个节点都要在服务端独立判定
- **数据范围与功能权限是两个维度**,缺一不可:
  能做什么由**角色**决定,能看谁的数据由**账号绑定的推广商**决定

## 2. 术语表

| 术语 | 定义 | 代码标识 |
|---|---|---|
| 后台账号 | 运营人员用来登录后台的账号,**不是玩家** | 后台账号 |
| 角色 | 一组被勾选的权限节点。账号通过绑定角色获得功能权限 | 角色 |
| 权限节点 | 权限树上的一个可勾选项,四层结构见 `6.2-角色列表/权限模型.md` | 权限节点 |
| 推广商范围 | 账号能看到哪些推广商的数据。**挂在账号上,不在角色上** | 账号-推广商关联 |
| 操作日志 | 全站后台操作的留痕,按操作者、路径、请求参数与接口描述记录 | 操作日志 |
| 到期时间 | 账号的有效期,过期后不能登录 | 后台账号.到期时间 |

## 3. 依赖关系

**上游依赖(我引用)**
- 渠道代理:推广商列表(**本仓库范围外**),账号绑定数据范围时选它

**下游被依赖(谁引用我)** ← 改权限模型前先看这行
- **全部业务模块**。M2 用户管理、M5 财务管理已在各自 README 第 6 段声明权限节点;
  数据中心、玩法中心、运营管理、渠道管理、系统设置同理(**均在本仓库范围外**)
- 各模块页面的「可见范围过滤」依赖账号的推广商绑定

> **权限模型是全平台被依赖最广的一处。** 改四层树的层级、改角色与账号的绑定方式,
> 影响每一个模块的每一个页面,改动前必须逐个模块确认。

## 4. 数据模型

**拥有的数据**(owner=M6)

| 数据 | 说明 | 现状 |
|---|---|---|
| 后台账号 | 账号、昵称、邮箱、密码、状态、到期时间、IP白名单、谷歌验证配置 | 已有 |
| 角色 | 角色名称、角色类型、备注、创建人、创建运营商 | 已有 |
| 角色权限关联 | 角色勾中了哪些权限节点 | 已有 |
| 账号推广商关联 | 账号的数据范围,多对多 | 已有 |
| 操作日志 | 操作者、路径、请求参数、接口描述、时间 | 已有 |

**只读引用**:推广商(owner=渠道代理,**本仓库范围外**)。

## 5. 菜单结构与页面索引

```
账号管理
├─ 账号管理    → 6.1
├─ 角色列表    → 6.2
└─ 操作日志    → 6.3
```

| 编号 | 页面 | 文档 |
|---|---|---|
| 6.1 | 账号管理 | [阅读需求](6.1-账号管理/README.md) |
| 6.2 | 角色列表 | [阅读需求](6.2-角色列表/README.md) |
| 6.3 | 操作日志 | [阅读需求](6.3-操作日志/README.md) |

> **权限模型的定义在 `6.2-角色列表/权限模型.md`** —— 它是被角色列表页配置的东西,
> 归属那一页的辅助文件,与 `资金模型.md` 归属用户列表页同一个道理。
> 模块 README 只做索引,不承载定义。

## 6. 权限点汇总

**权限模型的定义在 [`6.2-角色列表/权限模型.md`](./6.2-角色列表/权限模型.md)** ——
四层树结构、授权规则、数据范围、判定规则与已知边界都在那里。本段只做节点索引。

| 权限码 | 树节点路径 | 名称 | 页面 |
|---|---|---|---|
| `permission` | 账号管理 | 模块入口 | — |
| `permission:account` | 账号管理/账号管理 | 页面访问 | 6.1 |
| `permission:account:create` | …/账号管理/新增 | 新增账号 | 6.1 |
| `permission:account:edit` | …/账号管理/编辑 | 编辑账号 | 6.1 |
| `permission:account:delete` | …/账号管理/删除 | 删除账号 | 6.1 |
| `permission:account:toggle` | …/账号管理/状态切换 | 启用停用 | 6.1 |
| `permission:account:view-code` | …/账号管理/查看验证码 | 查看谷歌验证码 | 6.1 |
| `permission:account:reset-code` | …/账号管理/重置验证码 | 重置谷歌验证码 | 6.1 |
| `permission:role` | 账号管理/角色列表 | 页面访问 | 6.2 |
| `permission:role:create` | …/角色列表/新增 | 新增角色 | 6.2 |
| `permission:role:edit` | …/角色列表/编辑 | 编辑角色 | 6.2 |
| `permission:role:delete` | …/角色列表/删除 | 删除角色 | 6.2 |
| `permission:log` | 账号管理/操作日志 | 页面访问 | 6.3 |

以上节点**全部已存在于线上权限树**,无缺口。操作日志是纯只读页,没有操作子节点。

**其余模块的节点在各自模块 README 第 6 段声明**,本模块不重复列举 ——
已接入的有 M2 用户管理与 M5 财务管理,其余模块在本仓库范围外。

## 7. 决策清单

| # | 规则主题 | 涉及范围 | 执行结论 |
|---|---|---|---|
| D18 | 「角色类型」与「账号类型」的语义 | 6.1 · 6.2 字段清单 | 角色类型是运营填写的分类名称,1至32字符,去首尾空格,不影响权限。账号类型筛选引用账号当前角色的类型去重值;不单独配置账号类型,不据类型授予权限。 |
| D19 | 权限树里「提交审查」对应什么动作 | M5 的 5.1 | 提交审查不作为独立业务动作开放,权限节点停用且不授予操作能力。提现使用5.8自动转人工及5.1审核入口,不增加第二条重复提交路径。 |
| D20 | 界面已有、权限树缺失的操作节点如何补齐 | M5 的 5.1 · 5.2 · 5.3 | **执行规则**。M5 已列出七个需新增节点(见 M5 第 6 段)。补节点的动作在本模块,补齐节奏由本模块统一安排 |
| D21 | 财务类账号是否强制开启二次验证 | 6.1-A6 | 拥有财务资金变更权限的账号必须绑定并启用二次验证后才能执行资金操作,不得关闭或绕过;其他账号默认关闭。财务角色授权即时生效,未绑定时仅允许完成绑定,不执行资金操作。 |
| D22 | 操作日志是否受推广商范围过滤 | 6.3-R1 | 日志查询与导出都按账号推广商范围过滤,平台级日志仅全平台管理账号可查;跨推广商审计通过明确授予全部对应范围执行,不设置隐式绕过。 |

> 各业务模块发现「界面有按钮但树里没节点」时,统一挂到 D20 下,不各自开条目。

> **编号从 D18 起**(2026-08-11 renumber):原为 D13–D17,与 M5 的 Crypto 决策撞号。
> 决策编号全仓唯一,新开条目取全仓最大号加一,不在模块内从头数。
