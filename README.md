# Bingo 运营后台产品文档

C 端产品模型已完成,本仓库解决**运营后台的用户管理(M2)、财务管理(M5)与账号管理(M6)**。
在现有 `bingo-backend/apps/admin-api`(NestJS + GraphQL + Prisma)上**扩建**,不另起炉灶。

## 范围

**在范围内**:M2 用户管理、M5 财务管理的需求文档;M6 账号管理**只有模块契约**
(它是全平台权限模型的唯一定义方,页面文档暂不写);以及写这些文档的规范。

**不在范围内**:其余模块(数据中心、玩法、代理、运营、系统设置)的文档不由本仓库承载;
设计交付物(Figma 变量、组件库、设计交接)与竞品调研底稿同样不由本仓库承载。
文档只回答「要什么」,不回答「长什么样」。

需求会引用范围外的能力(活动彩金倍数、有效投注判定、站内信等)——
这些以业务名称出现,标注「本仓库范围外」,不指向本仓库内的路径。

> **财务于 2026-08 收入本仓库**(此前声明为「永久移出」)。理由:资金语义的唯一出处
> `资金模型.md` 在本仓库,而财务页面正是它被消费的地方 —— 拆两个仓库,R6–R17 的引用立刻断链。
> 收入范围后,`资金模型.md` 里把钱包、账变流水、打码任务等标注为「本仓库范围外」的说法已同步修正。

## 快速导航

| 你是 | 从这里开始 |
|---|---|
| 新人 / 想看全貌 | `M2-用户管理/README.md`、`M5-财务管理/README.md`(模块契约) |
| 要碰钱的逻辑 | `M2-用户管理/2.1-用户列表/资金模型.md`(全平台资金语义唯一出处) |
| 要碰权限 | `M6-账号管理/README.md` 第 6 段(全平台权限模型唯一出处) |
| 后端 / 要建表 | 模块 README 的「数据模型」段(数据归属与读写边界) |
| 前端 / 设计 | 页面 README 的字段清单与状态清单 |
| 排期 / PM | 模块 README 的页面索引表(优先级 + 人日) |
| 要写文档 | **先读 `00-公共约定/README.md`** |

## 目录结构

```
├── 00-公共约定/
│   ├── README.md            文档规范 + 数据归属规则 + 字段命名约定
│   ├── 页面模板.md            页面 README 的 v3 骨架,新建页面从这里复制
│   └── 校验.py              CI 校验脚本
├── M2-用户管理/
│   ├── README.md            模块契约:定位/术语/依赖/数据模型/页面索引/权限点/决策清单
│   └── <编号>-<页面名>/       只有已开工的页面才建文件夹
│       ├── README.md        页面需求文档
│       ├── <子视图>.md       (可选)拆出的辅助文件
│       └── 截图/             (可选)引用到的界面截图
├── M5-财务管理/              结构同上
├── M6-账号管理/              **只有模块契约**,页面暂不写;权限模型定义在此
└── .github/workflows/       CI
```

**核心规则:文件夹 = 页面。** 严格两层(模块 / 页面),菜单分组只在模块 README 的树里表达。
**有内容才建文件夹** —— 未开工的页面只存在于模块 README 的页面索引表,不建空占位目录。

## 当前规模

| 项 | 数量 |
|---|---|
| 模块 | 1 |
| 页面 | 7(已开工 1 · 未开工 6) |
| 可验收需求 | 5 条 |
| 流程图 | 3 张 |
| 工作量 | **P0 15.5** + P2 6 = **21.5 人日** |

页面数、开工数与人日由 `校验.py` 从模块页面索引表汇总,以它的输出为准。

## 现状基线(2026-07 盘点)

- M2 在 `bingo-backend` 侧基本可用:12 个 resolver,查询与干预能力较全
- **等级/VIP 需改造**:双 VIP 轨道待合并(见模块 README 的决策清单)
- **身份体系三套并存**:实名认证 / 用户标签 / 身份信息,SSOT 未定,阻塞 2.1
- 封禁记录、点控记录为新增表

## 待补输入

- **人力配置**(几个后端/前端/测试、周有效工时)→ 没有它做不了甘特图
- **带数据的后台账号** → 补关联账户判定窗口、查询响应上限、导出承载量等真实规则数值,
  以及资产变动原因枚举、状态监控被截断的列

## 文档规范

完整规范见 `00-公共约定/README.md`,要点:

1. **文档进 repo,跟代码同 PR 改。** 字段/规则变了不更新文档的 PR 视为未完成。
2. **不写页面布局。** 只回答"要什么"(字段、行为、状态),不回答"长什么样"——那是 Figma 的职责。
3. **不使用 emoji。** 状态用文字(`已有`/`改造`/`新增`),强调用加粗。
4. **不写待探讨。** 需求文档陈述决策;未定处写成明确假设,阻塞项统一记在模块 README 的决策清单。
5. **人日守恒**:需求人日之和 == 模块页面索引的人日。
6. **规则数值放配置,不放文档。** 文档只记:有哪些规则、语义、在哪配、谁能改。

## 本地校验

```bash
python3 00-公共约定/校验.py    # 全量规则,已开工页面必须符合 v3
```

CI 两个 job:文档结构(校验.py,`--strict`)+ 流程图语法(mermaid-cli 逐张解析)。

## 页面规范版本

当前为 **v3**(12 段模板,含需求详述 6 栏、异常与边界、数据流向与系统串接)。
已开工的页面**全部已迁移**,CI 跑 `--strict`。
新建页面从 `00-公共约定/页面模板.md` 复制,填完即符合 v3。
版本由**段落结构**判定,不靠 frontmatter 标记 —— 段落名匹配 v3 即进入严格检查。
