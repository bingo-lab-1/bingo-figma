# Bingo 运营后台产品文档

C 端产品模型已完成,本仓库只解决**运营后台**。
在现有 `bingo-backend/apps/admin-api`(NestJS + GraphQL + Prisma)上**扩建**,不另起炉灶。

## 快速导航

| 你是 | 从这里开始 |
|---|---|
| 新人 / 想看全貌 | `00-产品全景图.md` → `01-模块总览.md` |
| 后端 / 要建表 | 各模块的 `表设计.prisma` |
| 前端 / 设计 | 页面 README 的字段清单与状态清单 · `design-tokens/` |
| 排期 / PM | 模块 README 的页面索引表(优先级 + 人日) |
| 要写文档 | **先读 `00-公共约定/README.md`** |

## 目录结构

```
├── 00-产品全景图.md          思维导图 + 钱的一生 + 模块依赖
├── 01-模块总览.md            模块索引 + 依赖总图 + 跨模块泳道图 + 决策清单
├── 00-公共约定/
│   ├── README.md            文档规范 + 跨模块引用规则 + 字段命名约定
│   ├── 公共枚举.prisma       跨模块共享枚举
│   └── 校验.py              CI 校验脚本
├── M1-数据中心/ … M8-系统设置/
│   ├── README.md            模块契约:定位/术语/依赖/数据模型/页面索引/权限点
│   ├── 表设计.prisma         本模块拥有的表 + 外部引用声明
│   └── <编号>-<页面名>/
│       ├── README.md        页面需求文档
│       └── DESIGN.md        (可选)该页设计说明
├── design-tokens/           设计令牌:Figma Variables 的源与 CSS 产物
├── 参考资料/竞品-aigobet/     竞品调研 = 参考输入,不是交付物
└── .github/workflows/       CI
```

**核心规则:文件夹 = 页面。** 严格两层(模块 / 页面),菜单分组只在模块 README 的树里表达。

## 当前规模

| 项 | 数量 |
|---|---|
| 模块 | 8 |
| 页面 | 56 |
| 可验收需求 | 139 条 |
| 流程图 | 13 张 |
| 工作量 | **P0 124.5** + P1 102 + P2 50 = **276.5 人日** |

数字均由文档自动汇总(模块页面索引表),不手工维护第二份。

## 现状基线(2026-07 盘点)

- 自身:59 个 prisma model / 36 个 admin resolver / 8 个域
- **最大缺口 M3 财务**:提现审核、充值订单、通道、冻结全缺(P0 42 人日)
- **M1 报表几乎为零**:`dashboard.resolver.ts` 仅 59 行
- **M4 活动是改造不是新建**:现有 9 个硬编码 config resolver 要合并进配置化引擎
- **M7 权限最完整**,基本可直接用

## 必须先补的两个字段

全库 grep 命中 0,排在所有财务功能之前做,否则后面全部返工。

| 缺口 | 后果 |
|---|---|
| `Wallet.frozenBalance` | 提现拒绝后冻不住钱,多路径解冻无处落地 |
| `WagerRequirement` | 有打码倍数配置但无进度追踪 → **彩金送出去可直接提走** |

## 待拍板决策

阻塞下游的 7 项统一记在 `01-模块总览.md` 末尾,**不散落在各页文档**。
其中前三项需要 CEO 参与:自营 vs 白标主线 · 双 VIP 是否合并 · 身份体系 SSOT。

## 待补输入

- **人力配置**(几个后端/前端/测试、周有效工时)→ 没有它做不了甘特图
- **带数据的后台账号** → 补提现审核阈值、活动打码倍数等真实规则数值

## 文档规范

完整规范见 `00-公共约定/README.md`,要点:

1. **文档进 repo,跟代码同 PR 改。** 字段/规则变了不更新文档的 PR 视为未完成。
2. **不写页面布局。** 只回答"要什么"(字段、行为、状态),不回答"长什么样"——那是 Figma 的职责。
3. **不使用 emoji。** 状态用文字(`已有`/`改造`/`新增`),强调用加粗。
4. **不写待探讨。** 需求文档陈述决策;未定处写成明确假设,阻塞项统一记在决策清单。
5. **人日守恒**:需求人日之和 == 模块页面索引的人日。
6. **规则数值放配置,不放文档。** 文档只记:有哪些规则、语义、在哪配、谁能改。

## 本地校验

```bash
python3 00-公共约定/校验.py            # 全量规则
python3 00-公共约定/校验.py --strict   # 要求所有页面迁移到 v3
```

CI 两个 job:文档结构(校验.py)+ 流程图语法(mermaid-cli 逐张解析)。

## 迁移状态

页面规范当前为 **v3**(11 段模板,含需求详述 7 栏、异常与边界、数据流向与系统串接)。
已迁移 **1 / 56**(`M2-用户管理/2.1-用户列表/` 为样板)。
迁移状态由**段落结构**判定,不靠 frontmatter 标记。
未迁移页面按旧规则校验,不阻断 CI;全部迁移后把 CI 换成 `--strict`。
