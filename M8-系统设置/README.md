---
模块: M8
名称: 系统设置
负责人: 待定
状态: 设计中
更新: 2026-08-14
---

# M8 · 系统设置

## 1. 定位与边界

**做什么**:全站系统级参数配置的归口。**首版只收「风控参数」一页**:同IP/同设备注册分段限制、
风控统计窗口、CPF 绑定账号上限 —— 对应现有后台「系统设置-其他设置」页的「审核相关」区块。

**收入本仓库的理由**:同IP/同设备统计窗口是 M2 用户详情「风控数据」计数的**唯一口径出处**;
注册多号限制是 M7 代理返佣防自邀刷佣的配套防线。配置与消费方拆在两个仓库,口径立刻断链 ——
与财务、代理收入本仓库同一个道理。

**不做什么**
- 不做 RTP 取值范围 —— 与风控参数在现有后台同屏,但归玩法/游戏域(**本仓库范围外**),不收入
- 不做现有后台系统设置下的其他页面(数值配置、下载记录)—— 未收入,需要时按页另立
- 不做注册与 CPF 绑定流程本身 —— 执行在平台玩家端(**范围外**),本模块只定义参数与判定口径
- 不做设备码采集 —— 归客户端(**范围外**),只读引用(D30)
- 不做登录环节的限制 —— 首版只管注册与 CPF 绑定;登录事件只用于统计展示

**铁律**
- **拦截必须留痕**:每次超限拒绝都要可查(时间、维度、命中段位、当时计数),
  否则误伤无法定位、申诉无法核实;不留痕的拦截不得上线
- 拦截提示不得向玩家暴露具体风控规则与阈值

## 2. 术语表

| 术语 | 定义 | 代码标识 |
|---|---|---|
| 设备码 | 客户端采集上报的设备标识,用于同设备判定;采集与生成规则归客户端(**范围外**,D30) | 设备码 |
| 注册事件 | 一次注册成功的留痕:玩家、IP、设备码、时间 —— 判定与统计的数据基础 | 注册事件 |
| 分段限制 | 「时间窗 + 人数上限」的组合,最多 3 段;任一段达到上限即超限 | — |
| 统计窗口 | 用户详情「风控数据」计数的检索时长,不参与拦截 | — |

## 3. 依赖关系

**上游依赖(我引用)**
- 平台注册/登录流程(**本仓库范围外**):注册与登录的事件留痕(IP、设备码、时间)——
  判定与统计都建立在这张事件流水上,上线前须实盘确认留痕已存在(8.1-A8)
- 客户端(**范围外**):设备码采集与上报(D30)
- M2 用户管理:玩家档案与 CPF 绑定关系
- M6 账号管理:权限模型与全站操作日志

**下游被依赖(谁引用我)** ← 改参数语义前先看这行
- 平台注册与 CPF 绑定流程(**范围外**):读拦截参数并执行(8.1-R3/R4)
- M2 用户详情「风控数据」:读统计窗口(8.1-R5、8.1-A4)

## 4. 数据模型

**拥有的数据**(owner=M8)

| 数据 | 说明 | 现状 |
|---|---|---|
| 风控参数配置 | 分段限制、统计窗口、CPF 上限,含修改历史 | 现有后台已有,新后台缺失 |

**只读引用**:注册/登录事件留痕(owner=平台注册流程,**范围外**)·
玩家档案与 CPF 绑定(owner=M2)· 设备码(owner=客户端,**范围外**)。

## 5. 菜单结构与页面索引

```
系统设置
└─ 风控参数    → 8.1
```

现有后台该菜单下还有数值配置、下载记录,其「其他设置」页还含 RTP 取值范围 ——
均未收入本模块,需要时按页另立,不在此预留编号。

| 编号 | 页面 | 路由 | 优先级 | 现状 | 人日 |
|---|---|---|---|---|---|
| 8.1 | 风控参数 | `/system/risk` | P0 | 缺失 | 3 |

> P0 的理由:同IP/同设备注册限制是 M7 代理返佣上线的防多号配套,排期须先于或与 7.1 同步。
> 拦截的执行在注册流程(范围外),人日只计配置页与口径供数;人日为初估,未与人力配置校准。

## 6. 权限点汇总

| 权限码 | 树节点路径 | 名称 | 页面 |
|---|---|---|---|
| `system` | 系统设置 | 模块入口 | — |
| `system:risk:view` | 系统设置/风控参数/查看 | 页面访问与查看 | 8.1 |
| `system:risk:edit` | 系统设置/风控参数/编辑 | 编辑参数 | 8.1 |

以上节点**全部新增**,线上权限树尚无系统设置分支。
授权层现状见 M6 README —— 服务端目前不做权限判定,本模块权限码同样先是文档约定。

## 7. 决策清单

| # | 议题 | 卡住谁 | 现状 |
|---|---|---|---|
| D29 | 注册超限后的动作 | 8.1-R3 | **待定**:拒绝注册 / 转人工审核 / 仅标记放行。现行后台区块名「审核相关」暗示与审核有关,截图无法证实;文档按**拒绝注册**假设写就(8.1-A6),业务确认后改 |
| D30 | 设备码口径 | 8.1 同设备两项 | **待定**:设备码的采集与生成规则归客户端,现无文档;缺设备码的注册按**跳过同设备判定**假设写就(8.1-A7)。规则落地前同设备限制不生效(见 8.1 状态清单) |

> 页面级假设记在 8.1 第 4 段 A 表;真正阻塞的才开决策条目。
