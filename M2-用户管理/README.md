---
模块: M2
名称: 用户管理
负责人: 待定
状态: 设计中
更新: 2026-08-14
---

# M2 · 用户管理

## 1. 定位与边界

**做什么**:玩家账号的查询、干预、分层。**用户 的唯一写入方。**

**不做什么**
- 不做资金操作(充值/提现/上下分归财务,**本仓库范围外**,本模块只读展示)
- 不做后台账号管理(那是权限模块管理运营人员的账号,**本仓库范围外**)
- 不做风控规则引擎(只提供档案数据,规则判定另议)

**铁律**:封禁/解封/调级等干预操作**必须**留痕(操作日志,本仓库范围外)。
标签自 2.5 起改为规则判定,不再有人工打标动作 —— 留痕对象变成**规则的变更**,
自动打标与摘标另记进出流水(见 2.5)。

## 2. 术语表

| 术语 | 定义 | 代码标识 |
|---|---|---|
| 玩家 | C 端用户 | 用户 |
| 后台账号 | 运营人员账号,**不是玩家** | 后台账号(本仓库范围外) |
| 基础指标 | 平台预置的最小数据单元,如充值金额、投注次数,**不含时间范围**。**运营不能新增**,清单见 2.5 `条件模型.md` 第 3 段。2026-08-17 前称「基础标签」(D33) | 基础指标 |
| 标签 | 运营圈人分类,**非风控结论**。自 2.5 起是**一到十个条件**(基础指标 + 时间范围 + 比较符 + 右值)以 AND 或 OR 组合成的具名规则加上命中它的玩家集合,**不能人工挂摘** | 用户标签 |
| 实名 | KYC 认证 | 实名认证 |
| 封禁 | 限制登录/出入金/投注 | 封禁记录 |
| 点控 | 赢率干预 | 点控记录 |

## 3. 依赖关系

只声明边界与读写方向,不在此重复定义它们的数据。标注「本仓库范围外」的才是范围外,
其余是仓库内的兄弟模块。

**上游依赖(我引用)**
- 代理渠道(**本仓库范围外**):渠道(注册渠道)、代理(上级代理)
- 财务(M5):钱包 账变记录(**只读**展示余额与账变)· 充值订单 提现订单(弹窗内的充提记录)。
  M2.2 是账变流水的唯一运营查询入口;M5 继续拥有数据,不再提供重复的「交易记录」页面
- 活动(M4):**VIP 等级**(只读)—— 2.1 的列表展示与筛选、2.5 的属性条件。
  等级序列与升降级规则归 M4,本模块不写入,见 `00-公共约定/README.md` 第二部分 ① 与 ④

**下游被依赖(谁引用我)** ← 改表前先看这行
- 财务(M5):用户(订单归属)、用户标签(提现风控与订单列表展示)
- 活动(M4):用户标签(活动领取门槛与定向,见 M4 的 D39 / D44 / D53)
- 玩法(**本仓库范围外**):用户 用户标签(活动资格圈人)
- 代理渠道:用户(代理下级、佣金归属)
- 运营:用户标签(弹窗/推送定向)
- 数据中心:用户 登录记录(拉新/留存)

> **用户 和 用户标签 是全平台被引用最多的表**,改结构影响面最大 ——
> 影响面落在本仓库看不到的地方,改动前必须走到那几个下游确认。

## 4. 数据模型

**拥有的表**

| 表 | 说明 | 现状 |
|---|---|---|
| 用户 | 玩家主表 | 已有 |
| 实名认证 | 实名资料与审核结果 | 已有 |
| 标签 / 标签组成 | 具名人群规则及其条件行(「基础指标 + 时间范围 比较符 右值」),含标签级 AND/OR 组合方式 | 需改造 · 现有标签表无组成概念,见第 7 段 D23、D33 |
| 标签命中关系 | 当前命中某标签的玩家集合,由重算维护 | 需改造 · 现有关联表无打标时间与来源 |
| 标签进出流水 | 每一次打标与摘标的记录,含触发原因与判定当时的值 | 新增 |
| 基础指标清单 | 可用于组成条件的基础指标、口径、可用时间范围与可比白名单 | 新增 |
| 登录记录 | 登录时间、IP 与平台 | 已有 |
| 注册IP记录 / 注册设备记录 | 注册 IP/设备 | 已有 |
| 身份信息(3 张) | 身份体系 | 需改造 · 与实名认证、用户标签重叠,见第 7 段 D2。其中的自动标识部分按 D23 迁入 2.5 规则标签后下线 |
| 提现账户 / 法币账户 / 加密账户 | 收款账户 | 已有 |
| 资金安全设置 / 资金汇总 | 资金安全/汇总 | 已有 |
| 封禁记录 | 封禁与解封流水及原因 | 新增 |
| 点控记录 | 对特定用户的赢率干预 | 新增 |

**只读引用**:钱包 账变记录(owner=财务)· 投注记录(owner=玩法)· 渠道 代理(owner=代理渠道)
—— 三者均在**本仓库范围外**。**VIP等级**(owner=M4)—— 仓库内,展示、筛选与属性条件用,不写入。

## 5. 菜单结构与页面索引

```
用户管理
├─ 用户列表            → 2.1
├─ 投注明细            → 2.3
├─ 账变记录            → 2.2
├─ 封禁列表            → 2.4
├─ 用户标签管理  (分组)
│   └─ 用户标签        → 2.5
├─ 玩家排行榜          → 2.7
└─ 点控记录            → 2.6
```


| 编号 | 页面 | 路由 | 优先级 | 现状 | 人日 |
|---|---|---|---|---|---|
| 2.1 | 用户列表与详情 | `/user/list` | **P0** | 已有 | 3 |
| 2.2 | 账变记录 | `/user/wallet` | **P0** | 唯一入口 · 需按新资金模型重做 | 3 |
| 2.3 | 投注明细 | `/user/bet-record` | **P0** | 已有 | 2 |
| 2.4 | 封禁列表 | `/user/ban` | **P0** | 缺失 | 5 |
| 2.5 | 用户标签 | `/user/tag` | **P0** | 需改造 · 改为规则化,见 D23 | 12 |
| 2.6 | 点控记录 | `/user/point-control` | P2 | 缺失 | 3 |
| 2.7 | 玩家排行榜 | `/user/rank` | P2 | 缺失 | 3 |

> **2.1 的 3 人日只含页面本身**(R1–R5:列表、筛选、列设置、详情弹窗、导出)。
> `资金模型.md` 里的 R6–R15 是全平台资金规则,实现主体在财务、玩法、系统设置与玩家端,
> **人日不归这一页**,由承接方各自排期。语义定义在 2.1,是因为这一页是它们被消费的地方。

**2.5 的 12 人日**是规则化改造的全量,不是原来那个标签字典页的量。构成:
标签组成编辑界面 3 · 试算 1.5 · 重算(事件触发、每日全量、差集比对、熔断)4 ·
基础指标清单接入 1.5 · 进出流水查询 1 · 旧标识迁移与下线 1。
按 D23 取消人工打标后,原「打标/移除标签」的界面工作量归零,不在此列。
**该估算按累计类 12 项做出,2026-08-17 的条件模型改造(D33)未计入** ——
时间切片的按日汇总补建、业务时区设置、可比白名单配置、AND/OR 均是增量,须重估。

**M2.2 唯一入口口径(2026-08-10 已定)**:保留会员管理的「账变记录」,移除财务管理的
「交易记录」。M2.2 至少提供用户、渠道、账变时间、关联单号、账变类型和币种筛选,
并展示用户、时间、类型、币种、变动金额、变动前余额与变动后余额。财务旧路由只能跳转到
本页且重新校验 `user:wallet:view`;不得继续维护第二套列表、字段、筛选或权限。

**M2.3 与详情弹窗 tab4 的分工(2026-08-17 已定)**:两处都展示单注明细,但**不是重复**,
入口方向相反,一句话:**拿着单号找人用 2.3,拿着人看注单用弹窗**。
- 投注明细(2.3)= **跨用户**的注单查询入口:按局号/订单号、游戏、时间查,不需要先知道玩家是谁,用户只是可选筛选项
- 弹窗 tab4 订单数据 = **单个玩家**的下钻视图,只看当前玩家
- 列与字段口径以 `2.1-用户列表/用户详情弹窗.md` 4.4 为**唯一出处**,2.3 引用、不重复定义
- 弹窗 tab4 提供「在投注明细中打开」,跳转 2.3 并预填该玩家为筛选条件

## 6. 权限点汇总

| 权限码 | 名称 | 页面 |
|---|---|---|
| `user:list:view` | 查看用户列表 | 2.1 |
| `user:detail:view` | 查看用户详情 | 2.1 |
| `user:export` | 导出用户数据 | 2.1 |
| `user:wallet:view` | 查看余额与账变 | 2.2 |
| `user:bet:view` | 查看投注明细 | 2.3 |
| `user:ban` | 封禁/解封 | 2.4 |
| `user:tag:view` | 查看标签与进出流水 | 2.5 |
| `user:tag:edit` | 新增/编辑标签规则,含试算 | 2.5 |
| `user:tag:toggle` | 启用/停用标签 | 2.5 |
| `user:tag:delete` | 删除标签 | 2.5 |
| `user:point-control:view` | 查看点控记录 | 2.6 |
| `user:rank:view` | 查看玩家排行榜 | 2.7 |

## 7. 决策清单

阻塞下游的决策**只记在这里**,不散落到各页文档。页面文档遇到未定处写成明确假设,
真正卡住的挂到这张表上。

| # | 议题 | 卡住谁 | 现状 |
|---|---|---|---|
| D1 | 双 VIP 是否合并 —— 充值VIP / 投注VIP 两条独立线,玩家会有两个 VIP 维度 | 2.1 的 VIP 筛选与展示 · 2.5 的属性条件 | **已定**(2026-09-01 业务确认):**合并为单一等级序列**,一个玩家只有一个 VIP 等级;充值金额与投注金额并列为该序列的升级门槛(两个门槛的组合方式为 AND 还是 OR 待定,挂在 M4)。同时已定 **VIP 归属迁往 M4 活动管理** —— 等级序列、升降级门槛、保级与降级规则由 M4 拥有并写入,M2 转为只读引用(展示、筛选与 2.5 属性条件)。理由:VIP 的消费方几乎全在活动侧(红包雨按等级分档、VIP 奖励、签到按等级分档),配置与消费同模块才改得动;拆两边维护必然分叉。归属表与依赖方向见 `../00-公共约定/README.md` 第二部分 ① 与 ④。后续 VIP 相关决策一律开在 M4,不再回本表 |
| D2 | 身份体系 SSOT —— 实名认证 / 用户标签 / 身份信息 三套并存,谁是主 | 2.1 用户列表与详情弹窗的身份字段 | **部分已定**。标签这一支由 D23 结掉:标签全部规则化,身份信息里的自动标识迁进来后下线。剩下的实名认证与身份信息其余部分谁是主,仍未定,需 CEO 参与 |
| D3 | 打码多笔并存规则与有效投注定义 | 2.1-R8、2.1-R9 | **已定**:每笔入账独立建任务、严格 FIFO、单笔投注连续冲抵多条,见 `2.1-用户列表/资金模型.md` |
| D4 | 后台是否仍展示真金/彩金构成 | 2.1 的余额列 · 弹窗 tab1/tab5/tab7 | **已定**:后台保留(账本派生视图,不可加减不作判定依据),玩家端不展示。见 `资金模型.md` 第 6 段 |
| D23 | 标签是否保留人工打标 | 2.5 全页 · 身份信息表的去留 | **已定**:全部规则化,取消人工打标,标签只由「基础标签 运算符 值」组合判定,单个标签最多 10 个基础标签。**代价已知并接受**:现存手工标签中能用基础标签表达的迁成组合,表达不了的(客户经理跟进中一类临时标记)一并废弃,运营从此不能给单个玩家临时挂标;硬编码的自动标识迁成标签后下线。迁移清单与下线时点尚未排 |
| D24 | 金额类基础标签的币种口径 | 2.5-A9 · 全部金额类基础标签 | **已定**(2026-08-11):统一按**标准计价币种 USD**(写死)判定。每笔账变在入账时刻按当时汇率换算,并在账变流水上记六项换算依据,累计口径从这里汇总。规则见 `2.1-用户列表/标准计价.md`。这是底层记账规则,**不另开界面**;换算依据随账变呈现在 2.2。标准币金额是只读派生视图,**不作任何资金判定依据** |
| D31 | 条件行右值是否支持基础指标 | 2.5 表单与重算 · 提现审核规则的「净取款玩家」条件(`../M5-财务管理/README.md` 第 5 段 5.8) | **已定**(2026-08-14,**2026-08-17 修订**):右值可为常量或**可比白名单内**的基础指标(见 2.5-A12),用于表达「累计充值金额 ≤ 累计提现金额」类人群。初版按**单位组**(金额组 / 次数组)判可比,2026-08-17 随条件模型改造(D33)改为**指标级白名单**:类型一致是必要条件不是充分条件,逐项名单由产品确认。两条边界不变:**标签不得引用标签**(防依赖环);右值带系数的比率条件(如 累计投注 ≥ 0.96 × 累计充值)**列二期**,它能覆盖盈利率、投充比类风控人群,但引入系数校验面,首版不做 |
| D33 | 标签条件模型改造 —— 指标与时间范围是否分离,时间切片与 OR 是否入首版 | 2.5 全页 · 资金汇总的按日切片取数 · 5.8 引用的标签口径 | **已定**(2026-08-17):「基础标签」改造为「基础指标 + 时间范围」组合出条件,时间范围支持 当前 / 今日 / 昨日 / 最近N天 / 累计,左右两侧可各配时间范围(**推翻 2.5 旧 A8「首版只做累计类」**);条件间支持标签级单选 AND / OR,不嵌套(**推翻「恒为并且」**);比较符扩为 `= ≠ > ≥ < ≤`;自然日切片统一按新增的**业务时区**设置(2.5-A13)计算。**代价已知**:按日切片是新增汇总取数需求,须补建;指标间四则运算、百分比、自定义公式仍不做 |
| D34 | 指标无数据的判定口径 | 2.5-R6 · 5.8 依赖命中集的自动审核 | **已定**(2026-08-17):求和/计数类指标**无记录按 0 参与判定** —— 从未充值的玩家会被「累计充值金额 < 100」类人群圈中,**属预期行为**,低充值人群本就包含从未充值者(**推翻旧版「缺失一律不命中」**);取数失败、待补算(2.5-A9)等真异常为 **NULL,该行判否**,不按 0 顶替。两类情况必须区分,见 2.5-E4 |
| D35 | 账户余额能否作为基础指标 | 2.5 `条件模型.md` 指标清单的「账户余额」行 · 其入金额组白名单 | 未定。多币种单钱包下玩家没有单一 USD 余额,须按当前汇率换算,与标准计价「换算结果不作资金判定依据」(`2.1-用户列表/标准计价.md` SE6)及预估余额口径不明冲突;拍板前该指标不开放 |
| D56 | 2.5 条件模型是否支持**属性条件** | 2.5 `条件模型.md` 第 3 段 · 2.5 表单与重算 · M4 活动的领取门槛 | **已定**(2026-09-01 业务确认):支持。条件行新增第二种形态「属性 比较符 值」,与「基础指标 + 时间范围」并列;属性是当前时点的受控值,**无时间范围**、不参与按日切片,比较符沿用 D33 的六种,与指标条件在同一张表内混排。**首批只开 VIP等级一项**(owner=M4,有序枚举);D33 移出的另外两项(渠道来源、是否测试账号)仍待产品确认,不随本条一并开放。**破环约束**:VIP 的判定输入不得包含用户标签,依赖链保持单向 `原始数据 → VIP 等级 → 用户标签 → 活动门槛`,已写进 `../00-公共约定/README.md` 第二部分 ④ 第 2 款。**代价已知**:VIP 变动后含 VIP 条件的标签要等下一次重算才更新命中集,不做即时重算,与其他条件同一口径 |

> D2 需要平台层面拍板,超出本模块范围;定不下来,2.1 的相关字段随时返工。
> D1 已于 2026-09-01 定案并把 VIP 归属迁往 M4,后续 VIP 决策开在 `../M4-活动管理/README.md` 第 7 段。
> D24 已于 2026-08-11 结掉。它是底层记账规则,实现主体在账变流水侧(owner=M5),
> 与 R6–R17 同理**人日不归任何一页**,由承接方排期。
> D3、D4、D23 已拍板,保留在此是因为它们曾阻塞下游或前后台口径不一致,记录结论以免重开。
